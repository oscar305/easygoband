<div class="max-w-6xl flex flex-wrap mx-auto">
	{% for item in advantages %}
		<div class="md:min-w-1/3 mb-16 flex-1">
			<img class="h-16 mx-auto" src="{{assetPath("images/#{item.img}")}}">
			<p class="font-header normal-case text-2xl mt-8 leading-snug px-16">
				{{item.title}}
			</p>
		</div>
	{% endfor %}
</div>
