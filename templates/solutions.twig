{% extends "layouts/base.twig" %}

{% block content %}
	<article class="bg-white">
		<section class="max-w-4xl mx-auto">
			<h1 class="text-center">{{post.title}}</h1>
			<div class="text-xl mt-8">{{post.content}}</div>
		</section>
	</article>
{% endblock %}
